{
  "$schema": "https://design-tokens.github.io/community-group/format/",
  "_meta": {
    "name": "OwnPulse Design Tokens",
    "description": "Canonical, versioned source of truth for OwnPulse design tokens. Migrated verbatim from web/src/styles/variables.css. The chart.metric color slots feed the downstream Style Dictionary pipeline and chart-color unit (B5).",
    "version": "1.0.0",
    "source": "web/src/styles/variables.css",
    "chartMetricColors": "Per-metric chart color slots under chart.metric. heart_rate, hrv, and weight reuse exact values from the existing palette (web/src/styles/variables.css), and the fallback cycle reuses web/src/components/explore/chartColors.ts (lowercased). sleep_duration, glucose, bp_systolic, and bp_diastolic are newly assigned from existing palette colors and the colorblind-safe Wong palette to give each metric a distinct, accessible hue. bp_systolic deliberately avoids the brand-primary hue (#c2654a) so chart series do not collide with brand-colored UI chrome."
  },
  "color": {
    "primary": {
      "default": { "value": "#b2573c" },
      "hover": { "value": "#9e4f38" },
      "light": { "value": "#f7eeec" }
    },
    "accent": {
      "default": { "value": "#377c7c" },
      "light": { "value": "#5aadad" },
      "dark": { "value": "#2d6b6b" }
    },
    "dimension": {
      "gold": { "value": "#c49a3c" },
      "sage": { "value": "#5a8a5a" },
      "purple": { "value": "#7b61c2" },
      "energy": {
        "value": "#a78333",
        "$description": "Check-in dimension color for Energy. Diverges from color.dimension.gold (#c49a3c) — the raw gold value clears only ~2.5:1 against the light surfaces, below the 3:1 WCAG AA graphical-object floor the ScoreRing ring/arc and SparklineRow line need. Darkened along the same hue to ~3.4:1 (surface.bg) / ~3.5:1 (surface.elevated); re-verify with tools/design-tokens/contrast.js if this value ever changes."
      },
      "mood": {
        "value": "#b2573c",
        "$description": "Check-in dimension color for Mood. Mirrors color.primary.default — a literal duplicate (not a DTCG alias; build.js reads original token values directly)."
      },
      "focus": {
        "value": "#377c7c",
        "$description": "Check-in dimension color for Focus. Mirrors color.accent.default — a literal duplicate (not a DTCG alias; build.js reads original token values directly)."
      },
      "recovery": {
        "value": "#5a8a5a",
        "$description": "Check-in dimension color for Recovery. Mirrors color.dimension.sage — a literal duplicate (not a DTCG alias; build.js reads original token values directly)."
      },
      "libido": {
        "value": "#7b61c2",
        "$description": "Check-in dimension color for Libido. Mirrors color.dimension.purple — a literal duplicate (not a DTCG alias; build.js reads original token values directly)."
      }
    },
    "neutral": {
      "900": { "value": "#1e1e1c" },
      "800": { "value": "#2d2d2a" },
      "700": { "value": "#44443f" },
      "600": { "value": "#5e5e57" },
      "500": { "value": "#7a7a72" },
      "400": { "value": "#9c9c93" },
      "300": { "value": "#c2c2b9" },
      "200": { "value": "#deded6" },
      "100": { "value": "#eeeeea" },
      "50": { "value": "#f7f7f4" }
    },
    "surface": {
      "bg": { "value": "#fafaf7" },
      "bg-warm": { "value": "#faf6f1" },
      "default": { "value": "#fafaf7" },
      "elevated": { "value": "#ffffff" }
    },
    "text": {
      "default": { "value": "#1e1e1c" },
      "secondary": { "value": "#5e5e57" },
      "muted": { "value": "#72726b" }
    },
    "border": {
      "default": { "value": "#deded6" },
      "strong": { "value": "#c2c2b9" }
    },
    "feedback": {
      "success": { "value": "#007555" },
      "warning": { "value": "#976900" },
      "error": { "value": "#a44900" },
      "error-light": { "value": "#f5dede" }
    }
  },
  "typography": {
    "font-family": {
      "display": { "value": "\"Source Serif 4\", Georgia, \"Times New Roman\", serif" },
      "body": { "value": "\"IBM Plex Sans\", -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, sans-serif" },
      "mono": { "value": "\"IBM Plex Mono\", ui-monospace, SFMono-Regular, \"SF Mono\", Menlo, monospace" }
    },
    "font-size": {
      "xs": { "value": "0.75rem" },
      "sm": { "value": "0.875rem" },
      "base": { "value": "1rem" },
      "lg": { "value": "1.125rem" },
      "xl": { "value": "1.25rem" },
      "2xl": { "value": "1.5rem" },
      "3xl": { "value": "2rem" },
      "4xl": { "value": "2.75rem" }
    }
  },
  "spacing": {
    "content-padding": { "value": "1.5rem" },
    "sidebar-width": { "value": "220px" },
    "content-max-width": { "value": "72rem" }
  },
  "radii": {
    "sm": { "value": "4px" },
    "md": { "value": "8px" },
    "lg": { "value": "12px" }
  },
  "shadow": {
    "sm": { "value": "0 1px 2px rgba(0, 0, 0, 0.06)" },
    "md": { "value": "0 4px 12px rgba(0, 0, 0, 0.08)" },
    "lg": { "value": "0 8px 24px rgba(0, 0, 0, 0.12)" }
  },
  "dark": {
    "_description": "Dark-mode overrides for the CSS custom properties in web/src/styles/_tokens.css. Each key here is the tail of the --color-*/--shadow-* variable it overrides (e.g. color.bg -> --color-bg). Migrated verbatim from web/src/styles/variables.css, which duplicated these values byte-for-byte in both the [data-theme=\"dark\"] block and the @media (prefers-color-scheme: dark) block — a single token source kills that drift risk. iOS's OPColor.darkBg/cardDark are also sourced from color.bg/color.surface-elevated here (see SWIFT_COLOR_MAP in build.js), so both platforms share the same dark values.",
    "color": {
      "link": {
        "value": "#d98a6e",
        "$description": "Lighter foreground accent so link text clears WCAG AA (>= 4.5:1) on the dark surfaces below — base color.primary.default (#b2573c) is only ~3.16:1 on dark.color.surface-elevated (#252523); this is ~5.71:1."
      },
      "link-hover": { "value": "#e0a18a" },
      "success-fg": {
        "value": "#3cb88a",
        "$description": "Lighter foreground accent so success-colored text clears WCAG AA on dark surfaces — base color.feedback.success (#007555) is only ~2.69:1 on dark.color.surface-elevated; this is ~6.15:1."
      },
      "bg": { "value": "#1a1a18" },
      "bg-warm": { "value": "#1a1a1e" },
      "surface": { "value": "#1a1a18" },
      "surface-elevated": { "value": "#252523" },
      "text": { "value": "#eeeeea" },
      "text-secondary": { "value": "#c2c2b9" },
      "text-muted": { "value": "#9c9c93" },
      "border": { "value": "#44443f" },
      "border-strong": { "value": "#5e5e57" },
      "error-light": { "value": "#3d2020" },
      "primary-light": {
        "value": "rgba(178, 87, 60, 0.15)",
        "$description": "Dark-mode tint fill behind primary-colored text/badges. Derived from the CURRENT color.primary.default (#b2573c = rgb(178, 87, 60)) at 15% alpha. The hand-written variables.css version of this had drifted to rgba(194, 101, 74, ...) — the OLD pre-AA-darkening primary (#c2654a) — after primary.default was darkened; re-derive from color.primary.default if it ever changes again."
      }
    },
    "shadow": {
      "sm": { "value": "0 1px 2px rgba(0, 0, 0, 0.2)" },
      "md": { "value": "0 4px 12px rgba(0, 0, 0, 0.3)" },
      "lg": { "value": "0 8px 24px rgba(0, 0, 0, 0.4)" }
    }
  },
  "chart": {
    "intervention": {
      "value": "#7b61c2",
      "$description": "Marker color for intervention (substance/medication/supplement) events overlaid on charts. Brand purple, replacing the off-palette flat-UI #9b59b6. Intervention markers are shape-distinct from series lines, so the hue overlap with color.dimension.libido is acceptable."
    },
    "metric": {
      "heart_rate": { "value": "#d55e00" },
      "hrv": { "value": "#009e73" },
      "sleep_duration": { "value": "#7b61c2" },
      "weight": { "value": "#c49a3c" },
      "glucose": { "value": "#0072b2" },
      "bp_systolic": { "value": "#cc79a7" },
      "bp_diastolic": { "value": "#56b4e9" },
      "fallback": {
        "value": [
          "#c2654a",
          "#e69f00",
          "#56b4e9",
          "#009e73",
          "#d4a017",
          "#0072b2",
          "#d55e00",
          "#cc79a7",
          "#5b8a72",
          "#88ccee",
          "#44aa99",
          "#ddcc77"
        ]
      }
    }
  }
}
