# Staging overrides — smaller resources, separate secrets.
credentialsSecret: ownpulse-staging-postgres-secrets

storage:
  size: 5Gi

resources:
  requests:
    memory: "128Mi"
    cpu: "50m"
  limits:
    memory: "256Mi"
    cpu: "250m"
