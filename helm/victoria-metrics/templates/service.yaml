apiVersion: v1
kind: Service
metadata:
  name: {{ include "victoria-metrics.fullname" . }}
  labels:
    {{- include "victoria-metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "victoria-metrics.selectorLabels" . | nindent 4 }}
