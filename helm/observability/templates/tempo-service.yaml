apiVersion: v1
kind: Service
metadata:
  name: {{ include "observability.componentName" (dict "component" "tempo" "context" $) }}
  labels:
    {{- include "observability.labels" (dict "component" "tempo" "context" $) | nindent 4 }}
spec:
  type: {{ .Values.tempo.service.type }}
  ports:
    - port: {{ .Values.tempo.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.tempo.service.otlpGrpcPort }}
      targetPort: otlp-grpc
      protocol: TCP
      name: otlp-grpc
    - port: {{ .Values.tempo.service.otlpHttpPort }}
      targetPort: otlp-http
      protocol: TCP
      name: otlp-http
  selector:
    {{- include "observability.selectorLabels" (dict "component" "tempo" "context" $) | nindent 4 }}
